[project]
name = "rag-gateway"
version = "0.1.0"
description = "Unified API Gateway for RAG microservices"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "httpx>=0.28.0",
]
